name: Swift

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
#     - name: Build
#       run: swift build -v
    - name: Run tests
      run:
      xcodebuild -project BetterMVC.xcodeproj -scheme BetterMVC -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 12,OS=14.5' test
